# ATOMIC Framework v11.0 - Requirements
# Termux Compatible (no C-extension builds required)
# Versions pinned to tested releases — update with care
#
# Last refreshed: 2026-05 — pin floors raised to current stable patch
# levels.  Use ``pip install -U -r requirements.txt`` after bumping.

# Core dependencies
requests>=2.34.2,<3.0.0
beautifulsoup4>=4.14.3,<5.0.0
urllib3>=2.7.0,<3.0.0

# Database
sqlalchemy>=2.0.49,<3.0.0

# Report generation
fpdf2>=2.8.7,<3.0.0

# JWT handling
PyJWT>=2.12.1,<3.0.0

# Networking
pysocks>=1.7.1,<2.0.0

# Web interface
# Flask 3.1.x is the current stable line.  We pin a floor of 3.1.3 (security
# fix release) and allow patch upgrades within the 3.1 series.  Werkzeug is a
# transitive dependency of Flask but we declare a floor here so that a stale
# global install on a developer machine doesn't shadow Flask's expected
# version.
flask>=3.1.3,<4.0.0
werkzeug>=3.1.0,<4.0.0
jinja2>=3.1.4,<4.0.0
itsdangerous>=2.2.0,<3.0.0
flask-socketio>=5.6.1,<6.0.0
flask-cors>=6.0.2,<7.0.0

# Utilities
colorama>=0.4.6,<1.0.0
tqdm>=4.67.3,<5.0.0

# DNS resolution
dnspython>=2.8.0,<3.0.0

# Packet-level network scanning
scapy>=2.7.0,<3.0.0

# YAML configuration
PyYAML>=6.0.3,<7.0.0

# ── v11.0 new optional dependencies ──────────────────────────────────
# Async HTTP engine (--async-mode)
#   pip install httpx>=0.27.0

# Distributed workers (--distribute / --worker)
#   pip install redis>=5.0.0

# Browser scanner (--browser with Playwright)
#   pip install playwright>=1.40.0 && playwright install chromium
# Or with Selenium:
#   pip install selenium>=4.16.0

# Plugin hot-reload (--hot-reload)
#   pip install watchdog>=4.0.0

# TOML config support on Python < 3.11
#   pip install tomli>=2.0.0

# OpenTelemetry tracing (structured logging spans)
#   pip install opentelemetry-api>=1.20.0 opentelemetry-sdk>=1.20.0
#   pip install opentelemetry-exporter-otlp>=1.20.0  # for Jaeger/Grafana

# ── Cloud LLM providers (optional) ──────────────────────────────────
# Multi-provider via LiteLLM (Anthropic / OpenAI / Gemini / Groq /
# OpenRouter / Ollama / Mistral / DeepSeek / Together / xAI / Azure /
# Bedrock — single SDK for all of them):
#   pip install litellm>=1.40.0
# Or just the native SDK for your provider:
#   pip install anthropic>=0.30.0     # --llm-provider anthropic
#   pip install openai>=1.30.0        # --llm-provider openai (and Groq/
#                                       DeepSeek/OpenRouter/Together/Ollama
#                                       via their OpenAI-compatible APIs)
# A plain HTTP fallback using ``requests`` is always available for
# OpenAI-compatible endpoints, so installing nothing extra still works
# for self-hosted Ollama / LM Studio / vLLM.

# Testing
pytest>=8.0.0
pytest-timeout>=2.3.1

# Optional: C-extension packages (may require compilation on some platforms)
# Install separately if needed:
#   pip install lxml>=4.9.0 cryptography>=38.0.0 paramiko>=2.12.0

# Optional: Async crawling (used by discovery module for high-performance crawling)
#   pip install aiohttp>=3.9.0

# Optional: Enhanced HTML parsing (discovery module uses lxml as BeautifulSoup backend)
#   pip install lxml>=4.9.0

